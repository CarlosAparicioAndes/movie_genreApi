services:
  - type: web
    name: movie-genre-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 2 --bind 0.0.0.0:$PORT --timeout 120 api:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
